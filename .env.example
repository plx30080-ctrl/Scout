# Azure Maps key for map tile rendering (client-side only)
# Get this from: Azure Portal → your Azure Maps resource → Authentication → Primary Key
REACT_APP_AZURE_MAPS_KEY=your-azure-maps-primary-key
